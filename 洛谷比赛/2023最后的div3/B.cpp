#include<bits/stdc++.h>
using namespace std;
signed main(){
	int T;
	scanf("%lld",&T);
	while(T--){
		int p,a,b,c;
		scanf("%lld%lld%lld%lld",&p,&a,&b,&c);
		if(b==0){
			printf("No\n");
		}else{
			printf("Yes\n");
		}
	}
	return 0;
}
